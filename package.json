{
  "name": "nextjs-api-geomaps",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "vercel-build": "prisma init && prisma generate && prisma migrate deploy && next build && next start"
  },
  "dependencies": {
    "firebase": "^11.1.0",
    "next": "^15.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
